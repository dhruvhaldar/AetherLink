project AetherLink is
   for Source_Dirs use ("src");
   for Object_Dir use "obj";
   for Main use ("main.adb", "benchmark_packet.adb");

   package Compiler is
      for Default_Switches ("Ada") use ("-gnatwa", "-gnat2012", "-g", "-gnatn", "-O3");
   end Compiler;
end AetherLink;
